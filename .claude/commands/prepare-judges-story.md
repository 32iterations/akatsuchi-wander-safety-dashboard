你要幫忙把這個系統包裝成一個「評審 3 分鐘就懂」的故事。

1. 閱讀 `app/page.tsx` 與 `app/admin/page.tsx` 的文字說明與排版。
2. 產出一份 6–8 句的 Demo 解說腳本（繁體中文）：
   - 對應畫面切換：主頁 hero → 3D 場景 → 風險卡片 → 管理端。
   - 每一句都對應畫面中的一個互動或亮點。
3. 視需要微調頁面上少量文字，使其與解說腳本更一致，但避免大幅改動排版。
4. 若有時間，建議在註解中標記：「適合切成短影片的分鏡點」。
